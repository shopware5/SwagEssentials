version: '2'

services:
  app_server:
    build: _dev-ops/docker/containers/php7
    volumes:
      - ~/.composer/cache:/var/www/composer-cache-dir
      - .:/project
      - .:/var/www/shopware
    links:
      - app_mysql:mysql
    environment:
      TERM: xterm
      COMPOSER_CACHE_DIR: /var/www/composer-cache-dir
    ports:
      - "8083:80"
    networks:
      sw_essentials_docker:
        ipv4_address: 10.123.123.46

  app_mysql:
    build: _dev-ops/docker/containers/mysql
    volumes:
      - ./_dev-ops/docker/_volumes/app-mysqldata:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: app
      MYSQL_PASSWORD: app
    networks:
      sw_essentials_docker:
        ipv4_address: 10.123.123.47

  app_es:
    build: _dev-ops/docker/containers/elastic-search
    volumes:
      - ./_dev-ops/docker/containers/elastic-search/elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml
      - ./_dev-ops/docker/_volumes/app-esdata:/usr/share/elasticsearch/data
    ports:
      - "9200:9200"
      - "9300:9300"
    environment:
      ES_JAVA_OPTS: "-Xmx256m -Xms256m"
    networks:
      sw_essentials_docker:
        ipv4_address: 10.123.123.48

  app_redis:
    build: _dev-ops/docker/containers/redis
    volumes:
      - ./_dev-ops/docker/_volumes/app-redisdata:/data
    networks:
      sw_essentials_docker:
        ipv4_address: 10.123.123.49

networks:
  sw_essentials_docker:
    driver: bridge
    ipam:
      driver: default
      config:
      - subnet: 10.123.123.0/24
        gateway: 10.123.123.1